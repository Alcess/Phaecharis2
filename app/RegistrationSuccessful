import React from 'react';
import { View, Text, StyleSheet, TouchableOpacity } from 'react-native';

export default function App() {
  return (
    <View style={styles.container}>
      {/* Success Message */}
      <Text style={styles.header}>Registration Success!</Text>
      <Text style={styles.subHeader}>
        Congratulations! Your account is fully registered.
      </Text>

      {/* Spacer */}
      <View style={styles.spacer} />

      {/* Buttons */}
      <TouchableOpacity style={styles.proceedButton}>
        <Text style={styles.buttonText}>PROCEED</Text>
      </TouchableOpacity>
      <TouchableOpacity style={styles.laterButton}>
        <Text style={styles.buttonText}>I’LL DO IT LATER</Text>
      </TouchableOpacity>
    </View>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#FFF',
    padding: 20,
  },
  header: {
    fontSize: 28,
    fontWeight: 'bold',
    color: '#4C2A00',
    marginBottom: 8,
    textAlign: 'center',
  },
  subHeader: {
    fontSize: 14,
    color: '#888888',
    textAlign: 'center',
    marginBottom: 20,
  },
  spacer: {
    height: 50, // Adds space where the image used to be
  },
  proceedButton: {
    backgroundColor: '#FFB84D',
    borderRadius: 8,
    paddingVertical: 12,
    width: '80%',
    alignItems: 'center',
    marginBottom: 10,
  },
  laterButton: {
    backgroundColor: '#FFB84D',
    borderRadius: 8,
    paddingVertical: 12,
    width: '80%',
    alignItems: 'center',
  },
  buttonText: {
    fontSize: 16,
    color: '#FFF',
    fontWeight: 'bold',
  },
});
