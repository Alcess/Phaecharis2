import React from 'react';
import { StyleSheet, Text, View, Image, TouchableOpacity, ScrollView } from 'react-native';

export default function App() {
  return (
    <ScrollView style={styles.container}>
      <Text style={styles.header}>Notifications</Text>

      {/* Notification 1 */}
      <View style={styles.card}>
        <Image
          source={{ uri: 'https://i.pravatar.cc/50?img=1' }}
          style={styles.profilePic}
        />
        <View style={styles.cardContent}>
          <Text style={styles.name}>Pusang Baddie</Text>
          <Text style={styles.description}>
            Introduction to QA Engineering, Game Development and Software...
          </Text>
          <View style={styles.buttonRow}>
            <TouchableOpacity style={styles.replyButton}>
              <Text style={styles.buttonText}>Reply</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.forwardButton}>
              <Text style={styles.buttonText}>Forward</Text>
            </TouchableOpacity>
          </View>
          <Text style={styles.date}>October 12, 2022 at 8:26 am.</Text>
        </View>
      </View>

      {/* Notification 2 */}
      <View style={styles.card}>
        <Image
          source={{ uri: 'https://i.pravatar.cc/50?img=2' }}
          style={styles.profilePic}
        />
        <View style={styles.cardContent}>
          <Text style={styles.name}>Ryan Gusleng</Text>
          <Text style={styles.description}>
            Free lectures related to data analytics, web development, and game.....
          </Text>
          <View style={styles.buttonRow}>
            <TouchableOpacity style={styles.replyButton}>
              <Text style={styles.buttonText}>Reply</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.forwardButton}>
              <Text style={styles.buttonText}>Forward</Text>
            </TouchableOpacity>
          </View>
          <Text style={styles.date}>October 12, 2022 at 8:26 am.</Text>
        </View>
      </View>

      {/* Notification 3 */}
      <View style={styles.card}>
        <Image
          source={{ uri: 'https://i.pravatar.cc/50?img=3' }}
          style={styles.profilePic}
        />
        <View style={styles.cardContent}>
          <Text style={styles.name}>Sundalong Kanin</Text>
          <Text style={styles.description}>
            Schedule for the upcoming interview, and seminars related to your...
          </Text>
          <View style={styles.buttonRow}>
            <TouchableOpacity style={styles.replyButton}>
              <Text style={styles.buttonText}>Reply</Text>
            </TouchableOpacity>
            <TouchableOpacity style={styles.forwardButton}>
              <Text style={styles.buttonText}>Forward</Text>
            </TouchableOpacity>
          </View>
          <Text style={styles.date}>December 32, 2024 at 4:00 pm.</Text>
        </View>
      </View>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  container: {
    flex: 1,
    backgroundColor: '#F9F9F9',
    padding: 16,
  },
  header: {
    fontSize: 24,
    fontWeight: 'bold',
    marginBottom: 20,
  },
  card: {
    flexDirection: 'row',
    backgroundColor: '#FFD37C',
    borderRadius: 15,
    padding: 12,
    marginBottom: 15,
    shadowColor: '#000',
    shadowOpacity: 0.1,
    shadowOffset: { width: 0, height: 2 },
    shadowRadius: 4,
    elevation: 3,
  },
  profilePic: {
    width: 50,
    height: 50,
    borderRadius: 25,
    marginRight: 12,
  },
  cardContent: {
    flex: 1,
  },
  name: {
    fontSize: 16,
    fontWeight: 'bold',
    marginBottom: 4,
  },
  description: {
    fontSize: 14,
    color: '#555',
    marginBottom: 8,
  },
  buttonRow: {
    flexDirection: 'row',
    gap: 10,
    marginBottom: 8,
  },
  replyButton: {
    backgroundColor: '#9DFF9D',
    borderRadius: 20,
    paddingHorizontal: 12,
    paddingVertical: 6,
  },
  forwardButton: {
    backgroundColor: '#71FBB3',
    borderRadius: 20,
    paddingHorizontal: 12,
    paddingVertical: 6,
  },
  buttonText: {
    fontSize: 14,
    color: '#000',
  },
  date: {
    fontSize: 12,
    color: '#777',
  },
});
